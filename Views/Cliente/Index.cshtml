@using ControladorVendasASP.NET.Models
@model List<Cliente>
@{
    ViewData["Title"] = "Clientes";
}

<div class="text-center">

    <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        <a class="btn btn-primary" role="button" asp-controller="Cliente" asp-action="InserirClientes">Adcionar novo Cliente</a>
    </div>
    <br>
    <h1 class="display-4">Listagem de Clientes</h1>
    
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nome</th>
                <th scope="col">RG</th>
                <th scope="col">CPF</th>
                <th scope="col">E-Mail</th>
                <th scope="col">Telefone</th>
                <th scope="col">Celular</th>
                <th scope="col">CEP</th>
                <th scope="col">Endereco</th>
                <th scope="col">Número</th>
                <th scope="col">Complemento</th>
                <th scope="col">Bairro</th>
                <th scope="col">Cidade</th>
                <th scope="col">Estado</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (Cliente cliente in Model)
                {
                    <tr>
                        <th scope="row">@cliente.Id</th>
                        <td>@cliente.Nome</td>
                        <td>@cliente.Rg</td>
                        <td>@cliente.Cpf </td>
                        <td>@cliente.Email </td>
                        <td>@cliente.Telefone </td>
                        <td>@cliente.Celular </td>
                        <td>@cliente.Cep </td>
                        <td>@cliente.Endereco </td>
                        <td>@cliente.Numero </td>
                        <td>@cliente.Complemento </td>
                        <td>@cliente.Bairro </td>
                        <td>@cliente.Cidade </td>
                        <td>@cliente.Estado </td>
                        <td>
                            <div class="btn-group" role="group">
                                <a role="button" class="btn btn-primary" asp-route-id="@cliente.Id" asp-controller="Cliente" asp-action="EditarClientes">Editar</a>
                                <a role="button" class="btn btn-danger" asp-route-id="@cliente.Id" asp-controller="Cliente" asp-action="RemoverConfirmacao">Remover</a>
                            </div>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
</div>
